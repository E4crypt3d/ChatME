# provider selection: openrouter or groq (defaults to openrouter)
provider=openrouter

# openrouter api key
openrouter_api_key=

# groq api key
groq_api_key=

# default model (depends on provider, defaults to openrouter/free)
model=

# debug mode: set to true for verbose logs
debug=false
